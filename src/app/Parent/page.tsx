import React from 'react'
import ChildComponent from "../Child/page"; // Import the correct component name

const ParentComponent = () => {
    return (
        <div>
            <ChildComponent />
        </div>
    )
}

export default ParentComponent;

